<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="../../public/css/background.css">
        <link rel="stylesheet" href="../../public/css/index.css">
        <title>✂️Snipr</title>
    </head>
    <body>
        <div class="container">
            <div class="row align-items-center justify-content-between pt-5 mb-5">
                <div class="row align-items-center justify-content-between">
                    <a href="/" style="text-decoration:none">
                        <h1 id="logo">✂️Snipr</h1>
                    </a>
                </div>
                <div>
                    <a href="/login" class="btn btn-outline-primary ml-auto mt-3" id="log-in-btn">Log In</a>
                    <a href="/sign_up" class="btn btn-outline-primary ml-auto mt-3" id="sign-in-btn">Sign Up</a>
                </div>
            </div>
            <form action="/" method="POST" class="my-4 form-inline">
                <label for="originalUrl" class="sr-only">Url</label>
                <input required placeholder="Url" type="url" name="originalUrl" id="originalUrl" class="form-control col mr-2">
                <button class="btn btn-success" type="submit">Shrink</button>
            </form>
            <p id="message">Temporary links remain active for a duration of <span style="color: #B71C1C">3 days</span>. To generate permanent links, please <a href="/login" id="log-in-text">log in</a>.</p>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th class="table-head">Full URL</th>
                        <th class="table-head">Short URL</th>
                    </tr>
                </thead>
                <tbody>
                    <% tempUrls.forEach(tempUrl => { %>
                    <tr>
                        <td><a href="<%= tempUrl.originalUrl %>" target="_blank"  id="url"><%= tempUrl.originalUrl %></a></td>
                        <td><a href="/temp/<%= tempUrl.shortUrl %>" target="_blank" id="url"><%= tempUrl.shortUrl %></a></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </body>
</html>